def describe_worker_capabilities() -> str:
    return (
        "automation-worker skeleton: sync_orders, sync_items, sync_chats, "
        "send_reply, edit_item, adjust_price, publish_item, ship_order"
    )
